-----------WM OR DE SETUP -------------

#need xorg to begin for either WM or DE

######## MY DWM INSTALL ########

git clone https://github.com/Liam-Malone/configs
cd configs/Linux
check pkg_installs.txt to remove any unwanted packages or add desired packages
./installs.sh
you will be prompted to confirm some installs along the way
####### END OF DWM INSTALL #######

####### OTHERS #######

pacman -S xorg-server xorg-xinit
pacman -S xf86-video-intel


#### XMONAD INSTALL ####
$ sudo pacman -S \
> git \
> xorg-server xorg-apps xorg-xinit xorg-xmessage \
> libx11 libxft libxinerama libxrandr libxss \
> pkgconf

$ mkdir -p ~/.config/xmonad && cd ~/.config/xmonad
create a file xmonad.hs:
import XMonad

main :: IO ()
main = xmonad def

-------- 

$ git clone https://github.com/xmonad/xmonad
$ git clone https://github.com/xmonad/xmonad-contrib

$ sudo pacman -S stack

in ~/.config/xmonad:
	$ stack init

	$stack install  #Updates and recompiles all
###### END XMONAD INSTALL ######
